# Example commands

hayfind init-config
hayfind serve
hayfind index ~/projects/repo-a
hayfind search "vector database" --repo repo-a
hayfind search "fastapi" --path-prefix src/ --json
hayfind status
hayfind reindex
hayfind install-hooks
